# Web Components

Reusable React components for the web app belong here once a UI pattern is used in more than one feature or app surface.

Current rule: keep negotiation-specific UI in `src/features/negotiation` until reuse is proven.
