# Providers

External integration adapters live here.

- `openai.ts`: structured transcript extraction adapter with live JSON parsing + fixture fallback
- `tavily.ts`: vendor web verification adapter with live result mapping + fixture fallback
- `elevenlabs.ts`: subscription status and conversation transcript proof adapter
- `fixtures.ts`: deterministic extraction used when keys are absent
